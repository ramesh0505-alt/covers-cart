# Admin Portal — admin.coverscart.com
# Copy to .env.local for local development

# Backend API (production: your deployed API URL)
VITE_API_URL=https://api.coverscart.com/api

# Local development (with Vite proxy — leave as /api)
# VITE_API_URL=/api
